// backend/config/server.config.js
// require('dotenv').config();

module.exports = {
  port: process.env.PORT || 3000,
  // Add other server-specific configurations here
  // e.g. apiKeySecret: process.env.API_KEY_SECRET
}; 